<%= form_with(model: line_item, local: true) do |form| %>
  <% if line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_item.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
      <% line_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :line_item_order_id %>
  </div>

  <div class="field">
    <%= form.label :production_id %>
    <%= form.text_field :production_id, id: :line_item_production_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :line_item_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
